name: Build, Test and Deploy Leptos SSR App (leptos.rust-ui.com)

# Deploys rust-ui/leptos-ui to shared-apps as leptos-ui-app-1 on 127.0.0.1:4002.
# Host nginx routes leptos.rust-ui.com -> 127.0.0.1:4002.
# Requires Actions Variables `DOCKER_USERNAME`, `SERVER_IP`, `RUSTIFY_API_URL`.
# Requires Actions Secrets `DOCKER_TOKEN`, `RESEND_TOKEN`,
# `RESEND_AUDIENCE_ID`, `BUG_REPORTS_API_KEY`, and `SERVER_SSH_KEY`.
on:
  # 🖥️ gh workflow run prod-vps.yml
  # 🖥️ gh workflow run prod-vps.yml -f skip_build=true   ← redeploy only, no rebuild
  workflow_dispatch:
    inputs:
      skip_build:
        description: 'Skip build and redeploy current :latest image'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rustfmt

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install leptosfmt
        run: cargo binstall -y leptosfmt
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Check leptosfmt
        run: leptosfmt --check $(find . -name "*.rs" ! -path "./target/*")

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: clippy

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-clippy-${{ hashFiles('crates/tw_merge/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-clippy-

      - name: Clippy — tw_merge
        working-directory: crates/tw_merge
        run: cargo clippy --all-features -- -D warnings

  audit:
    name: Security Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install cargo-binstall
        uses: cargo-bins/cargo-binstall@main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Install cargo-audit
        run: cargo binstall -y cargo-audit
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run cargo audit
        run: cargo audit

  # tests: (disabled — already run locally via deploy_prod_vps.sh before triggering CI)
  #   name: Tests
  #   runs-on: ubuntu-latest
  #   steps:
  #     - uses: actions/checkout@v4
  #
  #     - name: Set up SSH for private git dependencies
  #       uses: webfactory/ssh-agent@v0.9.0
  #       with:
  #         ssh-private-key: ${{ secrets.GH_SSH_PRIVATE_KEY }}
  #
  #     - name: Install Rust toolchain
  #       uses: dtolnay/rust-toolchain@nightly
  #
  #     - name: Cache cargo
  #       uses: actions/cache@v4
  #       with:
  #         path: |
  #           ~/.cargo/registry
  #           ~/.cargo/git
  #         key: ${{ runner.os }}-cargo-test-${{ hashFiles('Cargo.lock') }}
  #         restore-keys: ${{ runner.os }}-cargo-test-
  #
  #     - name: Install cargo-binstall
  #       uses: cargo-bins/cargo-binstall@main
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Install cargo-nextest
  #       run: cargo binstall -y cargo-nextest
  #       env:
  #         GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  #
  #     - name: Run tests
  #       run: cargo nextest run

  typos:
    name: Typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: crate-ci/typos@master

  build:
    name: Build
    needs: [fmt, clippy, audit, typos]
    runs-on: ubuntu-latest
    if: github.event.inputs.skip_build != 'true'
    outputs:
      image_tag: ${{ steps.tag.outputs.sha }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Free disk space
      uses: jlumbroso/free-disk-space@main
      with:
        tool-cache: false
        android: true
        dotnet: true
        haskell: true
        large-packages: true
        docker-images: false
        swap-storage: false

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ vars.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_TOKEN }}

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Generate image tag
      id: tag
      run: echo "sha=${GITHUB_SHA::7}" >> $GITHUB_OUTPUT

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        file: ./Dockerfile
        push: true
        tags: |
          ${{ vars.DOCKER_USERNAME }}/leptos-ui:${{ steps.tag.outputs.sha }}
          ${{ vars.DOCKER_USERNAME }}/leptos-ui:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
        build-args: |
          RESEND_TOKEN=${{ secrets.RESEND_TOKEN }}
          RESEND_AUDIENCE_ID=${{ secrets.RESEND_AUDIENCE_ID }}
          RUSTIFY_API_URL=${{ vars.RUSTIFY_API_URL }}
          BUG_REPORTS_API_KEY=${{ secrets.BUG_REPORTS_API_KEY }}

  deploy:
    name: Deploy
    needs: [build]
    if: always() && (needs.build.result == 'success' || needs.build.result == 'skipped')
    runs-on: ubuntu-latest

    env:
      SSH: ssh -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no root@${{ vars.SERVER_IP }}
      SCP: scp -i ~/.ssh/deploy_key -o StrictHostKeyChecking=no

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup SSH key
      run: |
        mkdir -p ~/.ssh
        echo "${{ secrets.SERVER_SSH_KEY }}" > ~/.ssh/deploy_key
        chmod 600 ~/.ssh/deploy_key
        ssh-keyscan -H ${{ vars.SERVER_IP }} >> ~/.ssh/known_hosts

    - name: Copy compose file to server
      run: |
        ${{ env.SSH }} "mkdir -p ~/leptos-ui"
        ${{ env.SCP }} docker-compose.shared-server.yml root@${{ vars.SERVER_IP }}:~/leptos-ui/

    - name: Docker login on server
      run: |
        ${{ env.SSH }} "echo '${{ secrets.DOCKER_TOKEN }}' | docker login -u '${{ vars.DOCKER_USERNAME }}' --password-stdin"

    - name: Deploy app
      run: |
        ${{ env.SSH }} "cd ~/leptos-ui && export DOCKER_USERNAME=${{ vars.DOCKER_USERNAME }} && export DOCKER_REPOSITORY=leptos-ui && export IMAGE_TAG=latest && docker compose -f docker-compose.shared-server.yml down && docker rm -f leptos-ui-app-1 2>/dev/null || true && docker compose -f docker-compose.shared-server.yml pull && docker compose -f docker-compose.shared-server.yml up -d"

    - name: Smoke test
      run: |
        sleep 5
        ${{ env.SSH }} "curl -sf http://localhost:4002/ -o /dev/null || (docker compose -f ~/leptos-ui/docker-compose.shared-server.yml logs app --tail=20 && exit 1)"

    - name: Cleanup old Docker images
      run: |
        ${{ env.SSH }} "docker image prune -af"
