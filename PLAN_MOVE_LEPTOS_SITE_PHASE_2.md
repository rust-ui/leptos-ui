# Phase 2 — deploy from `leptos-ui`

Future plan. Keep minimal for now.

Goal: deploy website directly from `leptos-ui` repository.

Scope later:

- Move/adapt production CI/CD, Docker, VPS, Nginx, and release flow.
- Validate secrets, domains, health checks, SQLite/RUSTIFY persistence.
- Remove obsolete `app_crates/**` and temporary duplication after replacement structure is stable.
- Create migration checklist and rollback plan.

Do not execute in Phase 1.
